#!/bin/bash

######################
# Run correct script #
######################
case "$1" in
  cd)
    command=~/.bin/hub_cd
    ;;
  errors)
    command=~/.bin/hub_errors
    ;;
  mysql)
    command=~/.bin/hub_mysql
    ;;
  *)
    command=~/.bin/hub_set_env
    ;;
esac

. $command "$@"
